[project]
name = "pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.3",
    "pyarrow>=24.0.0",
    "click>=8.0.0",
    "sqlalchemy>=2.0.0",
    "psycopg>=3.0.0",
    "tqdm>=4.0.0",
]
